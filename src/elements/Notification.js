import styled from "styled-components"

export default styled.div`
    background: hsl(120, 60%, 60%);
    font-weight: 700;
    color: white;
    padding: 1rem 2rem;
    position: fixed;
    z-index: 10;
    top: 2rem;
    margin: 0 auto;
`